(small) if in plymouth I get password wrong, it asks me to type it twice, once for the full drive and once for the "disk root"
plymouth spinning nix logo with clean LY-like password entry 
server exclusive docker config (pihole, mini syncthing NAS, website hosting gits)
make a new host under aarch for the rpi. perhaps duplicate a specific config file for it because it is so different, and rename the current lowest tier to "base" rather than server.
find a way to do user passwd and create /home/user and chown user automatcially maybe using script
make personal NixOS liveusb on another usb and a specific setupscript for it.
